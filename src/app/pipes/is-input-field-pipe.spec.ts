import { IsInputFieldPipe } from './is-input-field-pipe';

describe('IsInputFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInputFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
