import { IsInputDetailPipe } from './is-input-detail-pipe';

describe('IsInputDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInputDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
