import { IsFormGroupArrayPipe } from './is-form-group-array-pipe';

describe('IsFormGroupArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormGroupArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
