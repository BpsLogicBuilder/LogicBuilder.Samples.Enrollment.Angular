import { IsGroupDetailPipe } from './is-group-detail-pipe';

describe('IsGroupDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new IsGroupDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
