import { IsGroupBoxPipe } from './is-group-box-pipe';

describe('IsGroupBoxPipe', () => {
  it('create an instance', () => {
    const pipe = new IsGroupBoxPipe();
    expect(pipe).toBeTruthy();
  });
});
