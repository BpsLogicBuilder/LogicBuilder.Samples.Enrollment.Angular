import { IsValueTextDetailPipe } from './is-value-text-detail-pipe';

describe('IsValueTextDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new IsValueTextDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
