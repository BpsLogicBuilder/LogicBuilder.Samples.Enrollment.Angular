import { IsDropdownSelectorPipe } from './is-dropdown-selector-pipe';

describe('IsDropdownSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new IsDropdownSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
