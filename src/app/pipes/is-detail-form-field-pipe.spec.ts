import { IsDetailFormFieldPipe } from './is-detail-form-field-pipe';

describe('IsDetailFormFieldPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IsDetailFormFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
