import { IsListDetailPipe } from './is-list-detail-pipe';

describe('IsListDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new IsListDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
