import { IsMultiSelectPipe } from './is-multi-select-pipe';

describe('IsMultiSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new IsMultiSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
